export const MESSAGE_REGISTRO = (formulario: string): string => {
  return `Se ha registrado correctamente ${formulario}`;
};

export const MESSAGE_ASIGNAR = (formulario: string): string => {
    return `Se ha asignado correctamente ${formulario}`;
  };

export const MESSAGE_EDITAR = (formulario: string): string => {
  return `Se ha actualizado correctamente ${formulario}`;
};

export const MESSAGE_ANULAR = (formulario: string): string => {
  return `Se ha anulado correctamente ${formulario}`;
};

export const MESSAGE_ELIMINAR = (formulario: string): string => {
    return `Se ha eliminado correctamente ${formulario}`;
  };

export const MESSAGE_HABILITADO = (formulario: string): string => {
  return `Se ha habilitado correctamente ${formulario}`;
};

export const MESSAGE_APROBADO = (formulario: string): string => {
  return `Se ha aprobado correctamente ${formulario}`;
};

export const MESSAGE_RECHAZADO = (formulario: string): string => {
  return `Se ha rechazado correctamente ${formulario}`;
};

export const MESSAGE_LISTANEGRA_ANULADO = (formulario: string): string => {
    return `Se ha retirado de la lista negra correctamente ${formulario}`;
  };

export const MESSAGE_LISTANEGRA = (): string => {
  return 'Esta persona está en la lista negra, por favor anúlela y vuelva a intentarlo';
};

export const MESSAGE_ERROR = (): string => {
  return 'Ocurrió un error inténtelo de nuevo';
};

export const MESSAGE_SUCCESS = (formulario: string): string => {
  return `Se ha registrado correctamente ${formulario}`;
};
